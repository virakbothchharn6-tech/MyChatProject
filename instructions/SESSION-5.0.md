# ChatSystem — Admin user management, role-based middleware, and Unicode correction

## Table of Contents

- [What Changed in Session 5.0](#what-changed-in-session-50)
- [File Contents](#file-contents)
  - [laravel-app/database/migrations/2026_07_05_055909_add_level_column_to_users_table.php](#laravel-appdatabasemigrations2026_07_05_055909_add_level_column_to_users_tablephp)
  - [laravel-app/app/Http/Middleware/AdminMiddleware.php](#laravel-appapphttpmiddlewareadminmiddlewarephp)
  - [laravel-app/app/Http/Middleware/UnicodeCorrection.php](#laravel-appapphttpmiddlewareunicodecorrectionphp)
  - [laravel-app/app/Http/Requests/User/GetUserRequest.php](#laravel-appapphttprequestsusergetuserrequestphp)
  - [laravel-app/app/Http/Requests/User/ReadUserRequest.php](#laravel-appapphttprequestsuserreaduserrequestphp)
  - [laravel-app/app/Http/Requests/User/CreateUserRequest.php](#laravel-appapphttprequestsusercreateuserrequestphp)
  - [laravel-app/app/Http/Requests/User/UpdateUserRequest.php](#laravel-appapphttprequestsuserupdateuserrequestphp)
  - [laravel-app/app/Http/Requests/User/DeleteUserRequest.php](#laravel-appapphttprequestsuserdeleteuserrequestphp)
  - [laravel-app/app/Http/Controllers/API/UserController.php](#laravel-appapphttpcontrollersapiusercontrollerphp)
  - [laravel-app/app/Models/User.php](#laravel-appappmodelsuserphp)
  - [laravel-app/app/Http/Resources/User/UserResource.php](#laravel-appapphttpresourcesuseruserresourcephp)
  - [laravel-app/bootstrap/app.php](#laravel-appbootstrapappphp)
  - [laravel-app/routes/api.php](#laravel-approutesapiphp)
  - [laravel-app/database/seeders/UserSeeder.php](#laravel-appdatabaseseedersuserseeederphp)
  - [laravel-app/database/seeders/DatabaseSeeder.php](#laravel-appdatabaseseedersdatabaseseederphp)
- [How Each File Works](#how-each-file-works)
  - [Migration — level column for user roles](#migration--level-column-for-user-roles)
  - [Middleware — admin authorization and Unicode normalization](#middleware--admin-authorization-and-unicode-normalization)
  - [Request validation — user CRUD operation rules](#request-validation--user-crud-operation-rules)
  - [UserController — admin-only user management endpoints](#usercontroller--admin-only-user-management-endpoints)
  - [User model and resource — role scopes and level field exposure](#user-model-and-resource--role-scopes-and-level-field-exposure)
  - [Routing and seeding — admin-guarded routes and sample user generation](#routing-and-seeding--admin-guarded-routes-and-sample-user-generation)
- [Common Commands](#common-commands)

---

## What Changed in Session 5.0

Session 4.4-frontend completed the profile management UI with password and image handling on the frontend. Session 5.0 introduces backend support for administrative user management by adding a `level` column to distinguish between `ADMIN` and `USER` roles, creating middleware to enforce admin-only access and normalize Unicode input for Khmer text, scaffolding request validation classes for user CRUD operations, implementing a `UserController` with paginated listing, create, update, and delete methods, extending the `User` model with role-based query scopes, registering admin-protected routes under `/api/manage/users`, and generating sample user data via a dedicated seeder.

| Area | Session 4.4-frontend | Session 5.0 |
|---|---|---|
| User roles | All users had identical permissions | Users now have `level` enum (`ADMIN`, `USER`) with database column and default value |
| Authorization | Only authentication was enforced | Introduces `AdminMiddleware` to block non-admin users from management endpoints |
| User resource fields | Included profile image and password state | Adds `level` field to API responses |
| User management | No administrative CRUD endpoints existed | Full REST API for admins to list, create, update, and delete users with pagination and search |
| Input preprocessing | No request transformation | `UnicodeCorrection` middleware normalizes Khmer Unicode sequences in all request data |
| Seeders | `DatabaseSeeder` created a single test user | Calls `UserSeeder` to generate 25 factory users with randomized data |

`laravel-app/database/migrations/2026_07_05_055909_add_level_column_to_users_table.php` was generated by command. `laravel-app/app/Http/Middleware/AdminMiddleware.php` was created manually. `laravel-app/app/Http/Middleware/UnicodeCorrection.php` was created manually. `laravel-app/app/Http/Requests/User/GetUserRequest.php` was generated by command, then manually edited. `laravel-app/app/Http/Requests/User/ReadUserRequest.php` was generated by command, then manually edited. `laravel-app/app/Http/Requests/User/CreateUserRequest.php` was generated by command, then manually edited. `laravel-app/app/Http/Requests/User/UpdateUserRequest.php` was generated by command, then manually edited. `laravel-app/app/Http/Requests/User/DeleteUserRequest.php` was generated by command, then manually edited. `laravel-app/app/Http/Controllers/API/UserController.php` was generated by command, then manually edited. `laravel-app/app/Models/User.php` existed previously and was edited manually to add role scopes and the `level` fillable attribute. `laravel-app/app/Http/Resources/User/UserResource.php` existed previously and was edited manually to include the `level` field. `laravel-app/bootstrap/app.php` existed previously and was edited manually to register the `admin` middleware alias. `laravel-app/routes/api.php` existed previously and was edited manually to add admin-protected user management routes. `laravel-app/database/seeders/UserSeeder.php` was generated by command, then manually edited. `laravel-app/database/seeders/DatabaseSeeder.php` existed previously and was edited manually to call `UserSeeder`.

---

## File Contents

The labels below each heading tell you what action to take:
- **Generated by command** — the file does not exist yet; run the command shown to let tooling create it.
- **Generated by command, then manually edited** — run the command first to scaffold the file, then paste the block to replace its contents.
- **Created manually** — the file does not exist and no CLI command creates it; paste the block to create it.
- **Edited manually** — the file already exists from a previous session; paste the block to replace its contents.

Follow the sections in order from top to bottom.

---

### `laravel-app/database/migrations/2026_07_05_055909_add_level_column_to_users_table.php`

> **Generated by command, then manually edited** — run the artisan command below to generate the migration file.

```bash
# Inside the laravel container
php artisan make:migration add_level_column_to_users_table
```

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration {
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::table('users', function (Blueprint $table) {
            $table->enum('level', ['ADMIN', 'USER'])->default('USER')->index('level');
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::table('users', function (Blueprint $table) {
            $table->dropColumn('level');
        });
    }
};
```

---

### `laravel-app/app/Http/Middleware/AdminMiddleware.php`

> **Generated by command, then manually edited** — run the artisan command below to generate middleware to restrict access to admin users only.

```bash
# Inside the laravel container
php artisan make:middleware AdminMiddleware
```

```php
<?php

namespace App\Http\Middleware;

use Closure;
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;

class AdminMiddleware
{
    /**
     * Handle an incoming request.
     *
     * @param  Closure(Request): (Response)  $next
     */
    public function handle(Request $request, Closure $next): Response
    {
        if (!$request->user() || $request->user()->level !== 'ADMIN') {
            return response([
                'message' => 'Unauthorized. Admin access required.'
            ], 403);
        }

        return $next($request);
    }
}
```

---

### `laravel-app/app/Http/Middleware/UnicodeCorrection.php`

> **Generated by command, then manually edited** — run the artisan command below to generate middleware to normalize Khmer Unicode character sequences in incoming request data.

```bash
# Inside the laravel container
php artisan make:middleware UnicodeCorrection
```

```php
<?php

namespace App\Http\Middleware;

use Closure;
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;

class UnicodeCorrection
{
    /**
     * Handle an incoming request.
     *
     * @param  Closure(Request): (Response)  $next
     */
    public function handle(Request $request, Closure $next): Response
    {

        $data = $request->all();

        $data = $this->replaceUnicodeRecursive($data);

        $request->replace($data);

        return $next($request);
    }

    private function replaceUnicodeRecursive($data)
    {
        if (is_array($data)) {
            foreach ($data as $key => $value) {
                $data[$key] = $this->replaceUnicodeRecursive($value);
            }
        } else {
            // Only process strings that are NOT base64/encoded
            if (is_string($data) && !$this->isSpecialString($data)) {
                $data = $this->replaceUnicode($data);
            }
        }
        return $data;
    }

    private function isSpecialString($string)
    {
        $trimmed = trim($string);

        // Skip empty strings
        if (empty($trimmed)) {
            return false;
        }

        // Skip JSON strings
        if ($trimmed[0] === '{' || $trimmed[0] === '[') {
            return true;
        }

        // Check for base64 pattern (mostly alphanumeric + / + = for padding)
        if (!preg_match('/^[a-zA-Z0-9+\/]*={0,2}$/', $string)) {
            return false;
        }

        // Check if it's valid base64 by decoding and re-encoding
        $decoded = base64_decode($string, true);
        if ($decoded === false) {
            return false;
        }

        // If re-encoded matches original, it's likely base64
        if (base64_encode($decoded) === $string) {
            return true;
        }

        return false;
    }

    private function replaceUnicode($text)
    {
        $text = trim($text);
        $text = preg_replace('/[\s]+/u', '\s', $text);
        $text = preg_replace('/[\t]+/u', '\t', $text);
        $text = preg_replace('/[\n]+/u', '\n', $text);
        $text = preg_replace('/[\r]+/u', '\r', $text);
        $text = preg_replace('/\x{200B}+/u', "\x{200B}", $text);

        $salabpi = ['ង', 'ញ', 'ប', 'ម', 'យ', 'រ', 'វ'];
        $treysab = ['ស', 'ហ', 'អ'];
        $chars = array_merge($salabpi, $treysab);
        $vowels = ['ិ', 'ី', 'ឹ', 'ឺ', 'ើ'];

        // Direct replacements
        $text = str_replace('្' . 'ដ', '្' . 'ត', $text);
        $text = str_replace('ា' . 'ំ', 'ាំ', $text);
        $text = str_replace('េ' . 'ី', 'ើ', $text);
        $text = str_replace('េ' . 'ា', 'ោ', $text);
        $text = str_replace('េ' . 'ះ', 'េះ', $text);
        $text = str_replace('ោ' . 'ះ', 'ោះ', $text);
        $text = str_replace('េ' . 'ុ' . 'ី', 'ុ' . 'ើ', $text);

        foreach ($chars as $char) {
            foreach ($vowels as $vowel) {
                if (in_array($char, $salabpi)) {
                    $replacementSign = '៉';
                } elseif (in_array($char, $treysab)) {
                    $replacementSign = '៊';
                } else {
                    continue;
                }
                $word = $char . 'ុ' . $vowel;
                $replacement = $char . $replacementSign . $vowel;
                $text = str_replace($word, $replacement, $text);
            }
        }
        return $text;
    }
}
```

---

### `laravel-app/app/Http/Requests/User/GetUserRequest.php`

> **Generated by command, then manually edited** — run the artisan command to scaffold the request, then replace its contents with the validation rules below.

```bash
# Inside the laravel container
php artisan make:request User/GetUserRequest
```

```php
<?php

namespace App\Http\Requests\User;

use Illuminate\Contracts\Validation\ValidationRule;
use Illuminate\Foundation\Http\FormRequest;

class GetUserRequest extends FormRequest
{
    /**
     * Determine if the user is authorized to make this request.
     */
    public function authorize(): bool
    {
        return true;
    }

    /**
     * Get the validation rules that apply to the request.
     *
     * @return array<string, ValidationRule|array<mixed>|string>
     */
    public function rules(): array
    {
        return [
            'keyword' => 'nullable|string|max:50',
            'per_page' => 'nullable|integer|in:10,25,50,100,250',
            'page' => 'nullable|integer|min:1',
        ];
    }
}
```

---

### `laravel-app/app/Http/Requests/User/ReadUserRequest.php`

> **Generated by command, then manually edited** — run the artisan command to scaffold the request, then replace its contents with the authorization logic below.

```bash
# Inside the laravel container
php artisan make:request User/ReadUserRequest
```

```php
<?php

namespace App\Http\Requests\User;

use Illuminate\Contracts\Validation\ValidationRule;
use Illuminate\Foundation\Http\FormRequest;

class ReadUserRequest extends FormRequest
{
    /**
     * Determine if the user is authorized to make this request.
     */
    public function authorize(): bool
    {
        return $this->user()->id !== (int) $this->route('id');
    }

    /**
     * Get the validation rules that apply to the request.
     *
     * @return array<string, ValidationRule|array<mixed>|string>
     */
    public function rules(): array
    {
        return [
            //
        ];
    }
}
```

---

### `laravel-app/app/Http/Requests/User/CreateUserRequest.php`

> **Generated by command, then manually edited** — run the artisan command to scaffold the request, then replace its contents with the validation rules below.

```bash
# Inside the laravel container
php artisan make:request User/CreateUserRequest
```

```php
<?php

namespace App\Http\Requests\User;

use Illuminate\Contracts\Validation\ValidationRule;
use Illuminate\Foundation\Http\FormRequest;

class CreateUserRequest extends FormRequest
{
    /**
     * Determine if the user is authorized to make this request.
     */
    public function authorize(): bool
    {
        return true;
    }

    /**
     * Get the validation rules that apply to the request.
     *
     * @return array<string, ValidationRule|array<mixed>|string>
     */
    public function rules(): array
    {
        return [
            'name' => 'required|string|max:50',
            'email' => 'required|email|unique:users,email',
            'password' => 'required|string|min:6|max:10'
        ];
    }
}
```

---

### `laravel-app/app/Http/Requests/User/UpdateUserRequest.php`

> **Generated by command, then manually edited** — run the artisan command to scaffold the request, then replace its contents with the validation rules and authorization logic below.

```bash
# Inside the laravel container
php artisan make:request User/UpdateUserRequest
```

```php
<?php

namespace App\Http\Requests\User;

use Illuminate\Contracts\Validation\ValidationRule;
use Illuminate\Foundation\Http\FormRequest;

class UpdateUserRequest extends FormRequest
{
    /**
     * Determine if the user is authorized to make this request.
     */
    public function authorize(): bool
    {
        return $this->user()->id !== (int) $this->route('id');
    }

    /**
     * Get the validation rules that apply to the request.
     *
     * @return array<string, ValidationRule|array<mixed>|string>
     */
    public function rules(): array
    {
        return [
            'name' => 'required|string|max:50',
            'email' => 'required|email|unique:users,email,' . $this->route('id'),
            'password' => 'nullable|string|min:6|max:10'
        ];
    }
}
```

---

### `laravel-app/app/Http/Requests/User/DeleteUserRequest.php`

> **Generated by command, then manually edited** — run the artisan command to scaffold the request, then replace its contents with the authorization logic below.

```bash
# Inside the laravel container
php artisan make:request User/DeleteUserRequest
```

```php
<?php

namespace App\Http\Requests\User;

use Illuminate\Contracts\Validation\ValidationRule;
use Illuminate\Foundation\Http\FormRequest;

class DeleteUserRequest extends FormRequest
{
    /**
     * Determine if the user is authorized to make this request.
     */
    public function authorize(): bool
    {
        return $this->user()->id !== (int) $this->route('id');
    }

    /**
     * Get the validation rules that apply to the request.
     *
     * @return array<string, ValidationRule|array<mixed>|string>
     */
    public function rules(): array
    {
        return [
            //
        ];
    }
}
```

---

### `laravel-app/app/Http/Controllers/API/UserController.php`

> **Generated by command, then manually edited** — run the artisan command to scaffold the controller, then replace its contents with the full CRUD implementation below.

```bash
# Inside the laravel container
php artisan make:controller API/UserController
```

```php
<?php

namespace App\Http\Controllers\API;

use App\Http\Controllers\Controller;
use App\Http\Requests\User\CreateUserRequest;
use App\Http\Requests\User\DeleteUserRequest;
use App\Http\Requests\User\GetUserRequest;
use App\Http\Requests\User\ReadUserRequest;
use App\Http\Requests\User\UpdateUserRequest;
use App\Http\Resources\User\UserResource;
use App\Models\User;
use DB;
use Exception;

class UserController extends Controller
{
    public function getUsers(GetUserRequest $request)
    {
        $keyword = $request->input('keyword', null);
        $perPage = $request->input('per_page', 25);
        $page = $request->input('page', 1);

        $users = User::isUser()
            ->when($keyword, function ($query, $keyword) {
                $query
                    ->where(function ($query) use ($keyword) {
                        $query->where('name', 'like', "%{$keyword}%")
                            ->orWhere('email', 'like', "%{$keyword}%");
                    });
            })->paginate($perPage, ['*'], 'page', $page);

        return response([
            'users' => UserResource::collection($users),
            'meta' => [
                'current_page' => $users->currentPage(),
                'last_page' => $users->lastPage(),
                'per_page' => $users->perPage(),
                'total' => $users->total(),
            ],
        ], 200);
    }

    public function readUser(ReadUserRequest $request)
    {
        $user = User::where('id', $request->route('id'))
            ->isUser()
            ->firstOrFail();
        return response([
            'user' => new UserResource($user)
        ], 200);
    }

    public function createUser(CreateUserRequest $request)
    {
        try {
            DB::beginTransaction();
            $user = User::create([
                'name' => $request->name,
                'email' => $request->email,
                'password' => $request->password,
                'level' => 'USER',
            ]);
            $user->markEmailAsVerified();
            DB::commit();
        } catch (Exception $e) {
            DB::rollBack();
            return response([
                'message' => 'Failed to create user'
            ], 500);
        }

        return response([
            'message' => 'User created.',
            'user' => new UserResource($user)
        ], 201);
    }

    public function updateUser(UpdateUserRequest $request)
    {
        $user = User::where('id', $request->route('id'))
            ->isUser()
            ->firstOrFail();

        try {
            DB::beginTransaction();
            $user->name = $request->name;
            $user->email = $request->email;
            if ($request->filled('password')) {
                $user->password = $request->password;
                $user->tokens()->delete(); // Invalidate all tokens of the user after update
            }
            $user->save();
            DB::commit();
        } catch (Exception $e) {
            DB::rollBack();
            return response([
                'message' => 'Failed to update user'
            ], 500);
        }

        return response([
            'message' => 'User updated.',
            'user' => new UserResource($user)
        ], 200);
    }

    public function deleteUser(DeleteUserRequest $request)
    {
        $user = User::where('id', $request->route('id'))
            ->isUser()
            ->firstOrFail();

        try {
            DB::beginTransaction();
            $user->tokens()->delete(); // Invalidate all tokens before deletion
            $user->delete();
            DB::commit();
        } catch (Exception $e) {
            DB::rollBack();
            return response([
                'message' => 'Failed to delete user'
            ], 500);
        }

        return response([
            'message' => 'User deleted.'
        ], 200);
    }
}
```

---

### `laravel-app/app/Models/User.php`

> **Edited manually** — update the User model to add the `level` fillable attribute and role-based query scopes.

```php
<?php

namespace App\Models;

// use Illuminate\Contracts\Auth\MustVerifyEmail;
use App\Notifications\EmailVerificationNotification;
use App\Notifications\ResetPasswordNotification;
use App\Services\ImageClassService;
use Database\Factories\UserFactory;
use Illuminate\Auth\MustVerifyEmail;
use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Hidden;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Database\Eloquent\Casts\Attribute;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Foundation\Auth\User as Authenticatable;
use Illuminate\Notifications\Notifiable;
use Laravel\Sanctum\HasApiTokens;

#[Fillable(['name', 'email', 'password', 'profile_image', 'level'])]
#[Hidden(['password', 'remember_token'])]
class User extends Authenticatable
{
    /** @use HasFactory<UserFactory> */
    use HasFactory, Notifiable, HasApiTokens, MustVerifyEmail;

    /**
     * Get the attributes that should be cast.
     *
     * @return array<string, string>
     */
    protected function casts(): array
    {
        return [
            'email_verified_at' => 'datetime',
            'password' => 'hashed',
        ];
    }

    public function sendEmailVerificationNotification($callback_url = null)
    {
        $this->notify(new EmailVerificationNotification($callback_url));
    }

    public function sendPasswordResetNotification($token, $callback_url = null)
    {
        $this->notify(new ResetPasswordNotification($token, $callback_url));
    }

    protected function passwordNull(): Attribute
    {
        return Attribute::make(
            get: fn() => empty($this->password),
        );
    }

    // profile image related methods and attributes
    protected function profileImage(): Attribute
    {
        return Attribute::make(
            get: function () {
                $imageClass = ImageClassService::forUserModel();
                $imagePath = $this->getRawOriginal('profile_image');
                return $imageClass->fullUrl($imagePath);
            },
        );
    }

    protected function profileThumbnail(): Attribute
    {
        return Attribute::make(
            get: function () {
                $imageClass = ImageClassService::forUserModel();
                $thumbnailPath = $imageClass->thumbnailPath($this->getRawOriginal('profile_image'));
                return $imageClass->fullUrl($thumbnailPath);
            },
        );
    }
    // end profile image related methods and attributes


    protected function scopeIsAdmin(Builder $query): void
    {
        $query->where('level', 'ADMIN');
    }

    protected function scopeIsUser(Builder $query): void
    {
        $query->where('level', 'USER');
    }
}
```

---

### `laravel-app/app/Http/Resources/User/UserResource.php`

> **Edited manually** — update the UserResource to expose the `level` field in API responses.

```php
<?php

namespace App\Http\Resources\User;

use Illuminate\Http\Request;
use Illuminate\Http\Resources\Json\JsonResource;

class UserResource extends JsonResource
{
    /**
     * Transform the resource into an array.
     *
     * @return array<string, mixed>
     */
    public function toArray(Request $request): array
    {
        return [
            'id' => $this->id,
            'name' => $this->name,
            'email' => $this->email,
            'profile_image' => $this->profile_image,
            'profile_thumbnail' => $this->profile_thumbnail,
            'password_null' => $this->password_null,
            'level' => $this->level,
        ];
    }
}
```

---

### `laravel-app/bootstrap/app.php`

> **Edited manually** — register the `admin` middleware alias so it can be used in route definitions.

```php
<?php

use App\Http\Middleware\AdminMiddleware;
use Illuminate\Foundation\Application;
use Illuminate\Foundation\Configuration\Exceptions;
use Illuminate\Foundation\Configuration\Middleware;

return Application::configure(basePath: dirname(__DIR__))
    ->withRouting(
        web: __DIR__ . '/../routes/web.php',
        api: __DIR__ . '/../routes/api.php',
        commands: __DIR__ . '/../routes/console.php',
        health: '/up',
    )
    ->withMiddleware(function (Middleware $middleware): void {
        $middleware->alias([
            'admin' => AdminMiddleware::class,
        ]);
    })
    ->withExceptions(function (Exceptions $exceptions): void {
        //
    })->create();
```

---

### `laravel-app/routes/api.php`

> **Edited manually** — add admin-protected user management routes under `/api/manage/users`.

```php
<?php

use App\Http\Controllers\API\AuthController;
use App\Http\Controllers\API\GoogleOAuthController;
use App\Http\Controllers\API\UserController;
use Illuminate\Support\Facades\Route;

Route::post('/signup', [AuthController::class, 'signup']);
Route::post('/signin', [AuthController::class, 'signin']);
Route::get('/verify/email/{id}/{hash}', [AuthController::class, 'verifyEmail'])
    ->middleware('signed')
    ->name('verify.email');
Route::post('/send/verification-email', [AuthController::class, 'sendVerificationEmail']);
Route::post('/send/reset-password-email', [AuthController::class, 'sendResetPasswordEmail']);
Route::post('/set/new-password', [AuthController::class, 'setNewPassword'])->name('set.new-password');

Route::prefix('google')->group(function () {
    Route::get('/oauth/redirect', [GoogleOAuthController::class, 'googleOAuthRedirect']);
    Route::get('/oauth/callback', [GoogleOAuthController::class, 'googleOAuthCallback']);
    Route::post('/oauth/exchange/token', [GoogleOAuthController::class, 'googleOAuthExchangeToken'])->middleware('auth:sanctum');
});

Route::middleware('auth:sanctum')->group(function () {
    Route::post('/signout', [AuthController::class, 'signout']);
    Route::get('/verify', [AuthController::class, 'verify']);
    Route::put('/create/password', [AuthController::class, 'createPassword']);
    Route::put('/change/password', [AuthController::class, 'changePassword']);
    Route::put('/update/profile-image', [AuthController::class, 'updateProfileImage']);
    Route::delete('/delete/profile-image', [AuthController::class, 'deleteProfileImage']);

    Route::middleware('admin')->prefix('manage')->group(function () {
        Route::prefix('users')->group(function () {
            Route::get('/', [UserController::class, 'getUsers']);
            Route::get('/read/{id}', [UserController::class, 'readUser']);
            Route::post('/create', [UserController::class, 'createUser']);
            Route::put('/update/{id}', [UserController::class, 'updateUser']);
            Route::delete('/delete/{id}', [UserController::class, 'deleteUser']);
        });
    });
});
```

---

### `laravel-app/database/seeders/UserSeeder.php`

> **Generated by command, then manually edited** — run the artisan command to scaffold the seeder, then replace its contents with the factory call below.

```bash
# Inside the laravel container
php artisan make:seeder UserSeeder
```

```php
<?php

namespace Database\Seeders;

use App\Models\User;
use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

class UserSeeder extends Seeder
{
    use WithoutModelEvents;

    /**
     * Run the database seeds.
     */
    public function run(): void
    {
        // Create 25 users
        User::factory(25)->create();
    }
}
```

---

### `laravel-app/database/seeders/DatabaseSeeder.php`

> **Edited manually** — replace the single test user creation with a call to `UserSeeder`.

```php
<?php

namespace Database\Seeders;

use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    use WithoutModelEvents;

    /**
     * Seed the application's database.
     */
    public function run(): void
    {
        $this->call([
            UserSeeder::class,
        ]);
    }
}
```

---

## How Each File Works

### Migration — level column for user roles

The migration adds an `enum` column named `level` with values `ADMIN` and `USER`, defaulting to `USER`, and creates an index on it for efficient role-based queries. The `down` method drops the column to allow rollback.

---

### Middleware — admin authorization and Unicode normalization

`AdminMiddleware` checks if the authenticated user's `level` is `ADMIN`; if not, it returns a 403 Forbidden response. `UnicodeCorrection` recursively processes all incoming request data, skipping base64-encoded and JSON strings, and normalizes Khmer Unicode by collapsing whitespace, correcting consonant-vowel-sign sequences, and replacing certain character combinations with canonical forms — this ensures database consistency for Khmer text input.

---

### Request validation — user CRUD operation rules

Each request class defines validation rules and authorization logic:
- `GetUserRequest` accepts optional keyword search, pagination size, and page number.
- `CreateUserRequest` requires name, unique email, and password with length constraints.
- `UpdateUserRequest` requires name, unique email (excluding the current user), and optional password; authorization prevents users from editing themselves.
- `ReadUserRequest` and `DeleteUserRequest` authorize only if the target user is not the authenticated user, preventing self-read or self-delete in the admin panel.

---

### UserController — admin-only user management endpoints

`UserController` provides five methods:
1. `getUsers`: returns paginated list of users with `level = USER`, supporting keyword search on name and email.
2. `readUser`: fetches a single user by ID.
3. `createUser`: creates a new user, marks email as verified, and returns a 201 response.
4. `updateUser`: updates user fields; if password is provided, invalidates all existing tokens to force re-login.
5. `deleteUser`: deletes the user and invalidates tokens first.

All operations are wrapped in database transactions and filter queries using the `isUser()` scope to exclude admin accounts from management.

---

### User model and resource — role scopes and level field exposure

The `User` model now includes `level` in the fillable array and defines two local query scopes: `isAdmin()` and `isUser()`, which filter by the `level` column. `UserResource` adds `level` to the JSON response so frontend components can conditionally render admin-only UI elements.

---

### Routing and seeding — admin-guarded routes and sample user generation

The `api.php` routes file wraps the new `/manage/users/*` endpoints in both `auth:sanctum` and `admin` middleware, ensuring only authenticated admin users can access them. `DatabaseSeeder` now calls `UserSeeder`, which generates 25 randomized user records via the existing factory, providing sample data for testing the pagination and search features.

---

## Common Commands

```bash
# Run the migration to add the level column
docker exec -it laravel-container php artisan migrate

# Seed the database with 25 sample users
docker exec -it laravel-container php artisan db:seed

# Rollback the last migration (removes level column)
docker exec -it laravel-container php artisan migrate:rollback

# Create a new migration file
docker exec -it laravel-container php artisan make:migration add_level_column_to_users_table

# Create a new request class
docker exec -it laravel-container php artisan make:request User/GetUserRequest

# Create a new controller
docker exec -it laravel-container php artisan make:controller API/UserController

# Create a new seeder
docker exec -it laravel-container php artisan make:seeder UserSeeder

# Start all services (Laravel + Vue.js + MySQL)
docker compose up

# Rebuild images and start all services (use after any Dockerfile or env file changes)
docker compose down && docker compose up --build

# Open a shell in the Laravel container to inspect the running environment
docker exec -it laravel-container sh
```
